#include <iostream>
// O #include é uma diretiva do Pré-processador.
// Ela é a PRIMEIRA fase do Processo de Compilação.

using namespace std;

// Este é o código-fonte (Source Code) que será COMPILADO
// em código de máquina (objeto) e, finalmente, LIGADO (Linked)
// com as bibliotecas (como o iostream) para criar o executável.
int main() {
    cout << "Fase de Compilação Concluída (Simbólica)." << endl;
    return 0;
}
